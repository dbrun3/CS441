model {
    hiddenSize = 256         // Increase hidden size for more learning capacity
    learnRate = 0.005        // Slightly higher learning rate to improve learning
    batchSize = 32           // Larger batch size to improve stability and learning
    windowSize = 5           // Keep larger context for each sentence
    numEpochs = 200          // Allow more epochs to fit the training dataochs to let the model learn more from the limited data
}

spark {
  averagingFrequency = 5
  workerPrefetchNumBatches = 5
}